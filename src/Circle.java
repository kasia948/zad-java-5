public class Circle {
    double r;


    double calculateArea (double r) {
        return Math.PI* Math.pow(r,2);
    }
    double calculatePerimeter (double r) {
        return 2*Math.PI*r;
    }
}

